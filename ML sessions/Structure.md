# The ML files structure

The file starts with the libraries import 
then the dataset,
then the preparation of algorithm if needed,
then the algorithm itself and explanation for every thing in algorithm,
lastly the prediction for a new value.